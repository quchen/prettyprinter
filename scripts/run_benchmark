#!/usr/bin/env bash

set -euo pipefail
set -v

cd "$(git rev-parse --show-toplevel)"
pwd

mkdir -p generated
stack bench wl-pprint \
    --benchmark-arguments "--output ../generated/wl-pprint-benchmark.html"
